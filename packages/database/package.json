{
  "name": "@viberoom/database",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "build": "prisma generate",
    "generate": "prisma generate",
    "push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "typescript": "^5.3.3"
  }
}
